# Hybrid Network Intrusion Detection System

## Overview
A hybrid NIDS combining Random Forest (known attacks) and Isolation Forest (zero-day anomalies). Includes a web interface for testing.

## Setup
1. Place NSL-KDD dataset files (KDDTrain+.txt, KDDTest+.txt) in `data/`.
2. Run preprocessing and feature selection: